module tst

enum Enum {
    Value1 = 0,
    Value2 = 777,
    Value3 = 543543,
    Value4 = -1,
}

variant Variant {
    Constant,
    Tuple(u8, i16, u32, [[i64; 3]; 5], varuint),
    Struct { field1: u32, field2: *mut bool },
}

struct Struct {
    a1: bool,
    a2: i8,
    a3: i16,
    a4: i32,
    a5: i64,
    a6: u8,
    a7: u16,
    a8: u32,
    a9: u64,
    a10: varint,
    a11: varuint,
    en: Enum,
    arrayOfInts: [i32; 4],
    doubleArrayOfInts: [[i32; 6]; 4],
    tripleArrayOfEnum: [[[Enum; 6]; 4]; 99],
    a12: [varuint; 11],
    a13: &Fn(i32) -> i64,
}

impl Variant {
    fn asd(e: Enum )
}
